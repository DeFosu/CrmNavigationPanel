import { DiReact } from "react-icons/di";
import React from "react";

const Logo: React.FC = () => {
    return (
        <div>
            <DiReact size={40} />
        </div>
    );
};

export default Logo;
